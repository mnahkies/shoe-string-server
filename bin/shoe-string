#!/usr/bin/env bash

# Wrapper script for git repository based installations,
# to ensure that the mise managed environment is used.

set -euo pipefail

SCRIPT_DIR="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
cd "$SCRIPT_DIR/.."

exec mise exec -- node ./dist/cli.mjs "$@"
